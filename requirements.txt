# Python Dependencies for Ralph Ollama Integration
#
# Install with: pip install -r requirements.txt

# HTTP requests for Ollama API
requests>=2.31.0

# Web UI (optional - only needed for ui/app.py)
flask>=2.3.0
flask-cors>=4.0.0

# Browser automation for E2E tests (optional - only needed for browser tests)
playwright>=1.40.0

# Note: Python 3.8+ required
# 
# After installing requirements, install Playwright browsers:
# playwright install chromium